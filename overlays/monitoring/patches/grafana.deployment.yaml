apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: monitoring
spec:
  template:
    spec:
      volumes:
      - emptyDir: null
        name: grafana-storage
        persistentVolumeClaim:
          claimName: grafana-storage
